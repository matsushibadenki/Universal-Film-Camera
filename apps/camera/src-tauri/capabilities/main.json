{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum core access for the bundled camera control UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

